package io.pello.simplest;

import com.google.inject.AbstractModule;

public class SimplestModule extends AbstractModule {
	  @Override 
	  protected void configure() {
	    //bind(io.pello.cleanbank.usecase.Login.class).to(io.pello.cleanbank.boundaries.LoginBoundary.class);
	  }
}